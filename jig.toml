# Worktree configuration
[worktree]
base = "origin/main"       # Base branch for new worktrees
on_create = "make install"  # Command to run after worktree creation
# copy = [".env"]            # Gitignored files to copy to new worktrees

# Spawn configuration
[spawn]
auto = true

# Agent configuration
[agent]
type = "claude"
